// Re-export project-related types from lib/types for convenience
export type {
  Project,
  ProjectDetail,
  ProjectPhoto,
  PhotoMedia,
  PhotoReference,
  UserProfileProject,
} from '@/app/lib/types';
