export interface Currency {
  id: number;
  name: string;
  code_iso: string;
}

export interface Country {
  id: number;
  name: string;
}

export interface Category {
  id: number;
  name: string;
}
